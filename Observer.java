// Sablonul de proiectare Observer pt. catalog.
// Parintii primesc notificare atunci cand copilul
// primeste nota de la profesor/asistent.
public interface Observer {
    void update(Notification notification);
}
